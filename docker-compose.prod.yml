# Production override - uses prebuilt images from GHCR
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  backend:
    image: ghcr.io/pinpoint-311/pinpoint-311-backend:latest
    build: !reset null
    volumes:
      - .:/project # Keep project mount for config access
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  worker:
    image: ghcr.io/pinpoint-311/pinpoint-311-backend:latest
    build: !reset null
    volumes: !reset []

  frontend:
    image: ghcr.io/pinpoint-311/pinpoint-311-frontend:latest
    build: !reset null
    volumes: !reset []
